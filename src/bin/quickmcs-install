#!/bin/env bash

if [[ x$ROOTPATH = "x" ]] then
  unset tmp
  tmp=${ROOTPATH=x}
  [[ $ROOTPATH = "x" ]] && ROOTPATH="/home/project/QuickMinecraftServer/src"
  unset tmp
fi
source ${ROOTPATH}usr/quickmcs/lib/quickmcslib


function loadOptions(){
local config="/home/Minecraft/Server/TBC/config.yml"
local HOST_PATTRN="  host: "
local hostArray=(`grep "$HOST_PATTRN" $config | sed 's/ //g;s/:/ /g'`)
ip=${hostArray[1]}
port=${hostarray[2]}
}

function main(){
echo "No more to do."
}


main


exit 0



